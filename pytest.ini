[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:documents
    error::DeprecationWarning:fill_forms
    error::DeprecationWarning:client_context
