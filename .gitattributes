# Normalize line endings so a diff is about content, not about CRLF.
* text=auto eol=lf
*.bat text eol=crlf
*.ico binary
*.pdf binary
*.xlsx binary
*.png binary

# A .command with CRLF fails on macOS with "bad interpreter" — force LF.
*.command text eol=lf
*.sh text eol=lf
